import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'VirtualScroll-TestApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
